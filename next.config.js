/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.immediate.co.uk", "https://unsplash.com/", "plus.unsplash.com", "images.unsplash.com"],
  },
}

module.exports = nextConfig
